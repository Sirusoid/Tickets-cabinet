# TODO

## BCC: тестирование после восстановления банка

Статус: подготовлено к прогонам. Таблица и поля фиксации готовы; тестирование начать с кейса 2.1 после доступности BCC.

### Таблица тест-кейсов для банка

Заполнять только используемые методы. Один прогон = одна строка. Секреты карты, MAC-ключи и персональные данные в этот файл не записывать.

| № | Название | Запрос | Финальный результат транзакции | Статус | Комментарии |
|---|---|---|---|---|---|
| 2.1 (*) | Покупка → CARD `TRTYPE=1` — Challenge Flow + Fingerprint | Ожидается | Ожидается `ACTION`, `RC`, `RRN`, `INT_REF` | Не пройден | Начинаем первым |
| 2.2 (*) | Покупка → CARD `TRTYPE=1` — Challenge Flow | Ожидается | Ожидается ответ BCC | Не пройден | После 2.1 |
| 2.3 (*) | Покупка → CARD `TRTYPE=1` — Frictionless Flow + Fingerprint | Ожидается | Ожидается ответ BCC | Не пройден | После 2.2 |
| 2.4 (*) | Покупка → CARD `TRTYPE=1` — Frictionless Flow | Ожидается | Ожидается ответ BCC | Не пройден | После 2.3 |
| 2.5 (*) | Покупка → CARD `TRTYPE=1` — NON3DSECURE | Ожидается | Ожидается ответ BCC | Не пройден | После 2.4 |
| `TRTYPE=14` | Возврат транзакции | Ожидаются `ORDER`, `ORG_AMOUNT`, `AMOUNT`, `RRN`, `INT_REF`, `P_SIGN` | Ожидается `ACTION=0`, `RC=00` | Не пройден | После успешной покупки |

### Что фиксировать в каждой строке

- параметры запроса: режим, `TRTYPE`, сумма, валюта, тестовый сценарий 3DS;
- `ORDER`, `ACTION`, `RC`, `RRN`, `INT_REF`, `P_SIGN` — сам `P_SIGN` можно записывать как `получен/проверен`, без значения;
- фактические URL `BACKREF` и `NOTIFY_URL` без секретов;
- результат банка: `Success`/`Fail` и текст ошибки;
- пришёл ли BACKREF и пришёл ли NOTIFY;
- создан ли билет и доступен ли PDF;
- создана ли запись в `cash_transactions`, её `payment_method`, `amount_cents`, `ticket_uids`;
- для `TRTYPE=14`: запись в `refunds`, `tickets.refund_status`, освобождение места и refund-транзакция.

### Уже подтверждено ранее

Был успешный прогон онлайн-покупки с `ACTION=0` и `RC=00`: билет и PDF создавались. Точный 3DS-профиль этого прогона не был зафиксирован, поэтому строки 2.1–2.5 им не закрываются.

Не заполнять: TOKEN, P2P, recurring, `TRTYPE=0`, двухстадийную оплату, `TRTYPE=12`, `TRTYPE=21`, `TRTYPE=22`.

## Онлайн-продажи и отчетность

- [x] Онлайн-финализатор пишет `cash_transactions` с `payment_method=card`, суммой в копейках, клиентом, `ORDER/RRN/INT_REF`, `ticket_uids` и нормализованным payload.
- [x] Отчет продаж считает выручку по `tickets`, а не складывает разноформатные `cash_transactions.amount_cents`.
- [x] В отчет добавлены разрезы по карте/наличным и возвраты.
- [ ] После следующей успешной оплаты проверить новую запись `cash_transactions` и отчет.
- [ ] Старые записи до нормализации не переписывать без отдельной сверки.

## Самостоятельный возврат

- [x] Реализован публичный возврат через BCC `TRTYPE=14`.
- [x] Добавлены настройки в разделе «Эквайр»:
  - `payments.bcc_self_refund_enabled`;
  - `payments.bcc_self_refund_window_minutes`;
  - `payments.bcc_self_refund_min_hours_before_event`.
- [x] По умолчанию рекомендовано окно 30 минут и ограничение за 2 часа до спектакля.
- [x] Для текущей политики срок возврата считается от начала спектакля минус настройка часов.
- [ ] После снятия лимитов BCC проверить реальный возврат `TRTYPE=14`.
- [ ] Проверить успешный и отклоненный ответы возврата, обновление `refunds`, `tickets.refund_status`, освобождение места и `cash_transactions`.

## Публичная страница заказа

- [x] Двуязычный интерфейс RU/KZ.
- [x] QR-код каждого билета.
- [x] Повторное открытие и скачивание PDF.
- [x] Формат даты, времени, названия спектакля, телефона, ряда и места.
- [x] Двуязычная памятка по возврату.
- [ ] Проверить печать после загрузки на сервере.

## Касса и виджет

- [x] В кассе проданные и зарезервированные места показываются цветом и номером.
- [x] В публичном виджете занятые места показываются базовым цветом без номера.
- [x] Для редактора сеанса отключено масштабирование колесом мыши.
- [x] На странице билетов добавлена колонка «Номер заказа».
- [ ] После деплоя проверить снятие резерва в `/cash/sell.php`.

## Деплой

- [ ] После финальных изменений загрузить файлы на сервер.
- [ ] Очистить OPcache.
- [ ] Не импортировать полный SQL-дамп в рабочую базу.
- [ ] Новые настройки создавать через «Настройки -> Эквайр» или точечный INSERT в `settings`.
